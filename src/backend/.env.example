# Example .env file for backend Node.js/Express.js application
# Copy this file to .env and adjust values as needed for your environment.

# PORT: The port the server listens on. Default is 3000. Cloud platforms will override this.
PORT=3000

# NODE_ENV: Set to 'development' for local dev, 'production' for production deployments.
NODE_ENV=development